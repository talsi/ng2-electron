/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { StepsPipe } from './steps.pipe';

describe('StepsPipe', () => {
  it('create an instance', () => {
    const pipe = new StepsPipe();
    expect(pipe).toBeTruthy();
  });
});
